{
  "name": "claw-incremental",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Prototype de machine a pince de fete foraine en pixel art, controles tactiles.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "check:art": "node scripts/check-art.mjs",
    "test": "playwright test",
    "check": "npm run typecheck && npm run check:art && npm run test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
